# Base URL of the LogSense backend API (see backend/app.py).
# For local development against `uvicorn backend.app:app`, the default below is fine.
# For a Kaggle/Colab-hosted backend, paste the ngrok URL printed when the server starts.
VITE_API_BASE_URL=http://localhost:8000
